namespace Settings.Application.DTOs.Numeration;

/// <summary>Predefined formula element tokens for dropdown ({{token}} format).</summary>
public static class FormulaElementOptions
{
    public static readonly string[] All =
    [
        "{{yyyy}}",
        "{{year}}",
        "{{yy}}",
        "{{mm}}",
        "{{month}}",
        "{{dd}}",
        "{{day}}",
        "{{index}}",
        "{{seq}}",
        "{{userCode}}",
        "{{employeeCode}}",
        "{{clientCode}}",
        "{{companyCode}}",
        "{{companyPrefix}}",
        "{{departmentPrefix}}",
        "{{departmentCode}}",
        "{{elementCode}}",
        "{{documentTypeCode}}",
        "{{numerationForCode}}",
    ];
}
